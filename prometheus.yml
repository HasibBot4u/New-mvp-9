global:
  scrape_interval: 15s

rule_files:
  - "alert.rules"

scrape_configs:
  - job_name: 'nexusedu-backend'
    static_configs:
      - targets: ['backend:8080']
  
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
